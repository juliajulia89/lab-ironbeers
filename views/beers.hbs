<section class = "section">
  {{#each this}}
    <img src='{{this.image_url}}' />
    <ul>
      <li> {{this.name}} </li>
      <li> {{this.description}} </li>
      <li> {{this.tagline}} </li>
    </ul>
  {{/each}}
</section>